services:
  zed2api:
    build: .
    container_name: zed2api
    ports:
      - "8000:8000"
    environment:
      ZED2API_PORT: 8000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
